services:
  - type: web
    name: fraud-detection-api
    runtime: python
    buildCommand: git lfs pull && bash scripts/fetch_model.sh && pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
